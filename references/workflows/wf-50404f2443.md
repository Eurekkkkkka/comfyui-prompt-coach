# 图生视频

工作流路径：`图生视频/图生视频.json`  
核对：2026-09-20｜以本卡实际节点为准｜按字段填写  
源文件 SHA-256：`87986ec137f37eec81592614b6a8cf5ef7fcca9b6c45ebe53bcd188c3f43e4ab`

## 用途与准备

从首帧或首尾两张图生成连续运动。
首帧作为开场构图；首尾帧变体再提供结尾图，两图身份、衣服、空间应一致。

## 本工作流先看

- 新版真正入口是 #286 CR Text.text，自动进入 #236.text。旧目录指向 #236 会改到已连接的内部编码框。#267 上传首帧，#206 为负向。

## 素材放在哪里

| 位置 | 素材 | 说明 |
|---|---|---|
| #267 LoadImage → `image` | 图片 | 替换示例素材并核对预览；多素材顺序按专用说明确认 |

## 提示词和填写位置

描述从画面初态出发的动作、镜头运动、环境变化和结尾状态；单镜头先只安排一个主动作。

| 实际输入位置 | 用途 | 怎么填 |
|---|---|---|
| #206 CLIPTextEncode → `text` | 负向 | 首次保留当前负向；只为实际问题追加短约束 |
| #286 文本 → `text` | 正向/编辑指令 | 按用户目标填写；正文不含节点说明或尺寸参数 |

已接线的文本框由上游提供，直接改内部旧值可能不生效：

- #236 CLIPTextEncode → `text` ← #286 文本。修改上游创作入口；若来源是自动分析/构建器，在其实际输入或界面中修改。

输入写法示例（用于说明表达方式；正式交付须替换为用户素材事实，并按本卡拆到对应字段）：

人物保持原有服装与位置，缓慢转头看向镜头，露出轻微微笑；镜头稳定缓慢推进，窗帘随微风轻动，背景保持连续。

## 参数与运行

1. 在工作流列表打开 `图生视频/图生视频.json`，确认名称和主要节点与本卡一致；缺节点时先处理加载问题。
2. 在上面列出的入口替换素材并查看预览；多图、多音色、首尾帧先绑定各自职责。
3. 按当前输入方式填写文本或操作时间线；无需提示词的工作流跳过文字生成。
4. 保留模型、采样器与内部系统模板；只修改本任务需要的尺寸、时长或强度。先做一张图或一小段音视频。
5. 点击 ComfyUI 的运行/队列按钮，等待输出；失败时读取第一个报错节点，缺文件先换有效素材，显存不足先降低尺寸或片段长度。

实际可调参数位置（以当前界面值为准，首次不要全部修改）：

- #248 WanMoeKSampler：`denoise`。
- #245 VHS_VideoCombine：`frame_rate`。
- #244 RIFE VFI：`multiplier`。

## 获取结果与验收

结果位置：#245 VHS_VideoCombine。在对应预览保存/下载，核对本次输出时间。
检查首帧继承、动作可达性、手脚结构和尾帧收敛；突然跳景时先减小动作量。

失败重跑：保留原结果与种子记录，只调整一个明确变量；先判断是输入/节点错误还是画面、声音效果问题。

本卡依据工作流 JSON、节点公开字段和连线编写；尚未对本工作流执行 GPU 生成验收。用户实际 JSON 或界面变化时，以当前文件为准并重新核对。
