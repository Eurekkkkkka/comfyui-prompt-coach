# 视频打光-文丨MiniMax

工作流路径：`光影处理/视频打光-文丨MiniMax.json`  
核对：2026-09-20｜H3｜按字段填写  
源文件 SHA-256：`0c9765a319e564eb52384dd94f476598f867508f5c7de746d9fb57c5dd596c05`

## 用途与准备

改变图片或视频的布光，保留主体与构图。
图片提供原图；视频提供源片。明确光源方位、软硬、色温与需要保留的人物。

## 本工作流先看

- #138.value 填修改要求；#216、#243 CR Text 是固定 H3 包装文字，保留。只改变光照，明确保持人物、动作和源片时序。
- 该路线直接将拼接文本送入 #136，不要把另一工作流的 PromptRewriter 节点位置套过来。

## 素材放在哪里

| 位置 | 素材 | 说明 |
|---|---|---|
| #141 加载视频 → `video` | 视频 | 实际连接标签：初始视频、音频；替换素材后核对预览 |

## 提示词和填写位置

写清主光来自哪里、照到哪里、阴影落在哪里；视频要求光照随动作连续，不新增物体。

先读取 [H3 适配](../minimax-h3-prompting.md)，按其入口完整读取 [子技能 SKILL](../minimax-h3-official/SKILL.md) 和 Base 或 Ref2VA 规范；本卡只负责输入框定位。

| 实际输入位置 | 用途 | 怎么填 |
|---|---|---|
| #216 CR Text → `text` | 文本（用途见专用说明） | H3 结构包装，保持预设 |
| #243 CR Text → `text` | 文本（用途见专用说明） | H3 结构包装，保持预设 |
| #138 提示词 → `value` | 文本（用途见专用说明） | 按用户目标填写；正文不含节点说明或尺寸参数 |

已接线的文本框由上游提供，直接改内部旧值可能不生效：

- #242 CR Text Concatenate → `text1` ← #215 CR Text Concatenate。修改上游创作入口；若来源是自动分析/构建器，在其实际输入或界面中修改。
- #242 CR Text Concatenate → `text2` ← #243 CR Text。修改上游创作入口；若来源是自动分析/构建器，在其实际输入或界面中修改。
- #215 CR Text Concatenate → `text1` ← #216 CR Text。修改上游创作入口；若来源是自动分析/构建器，在其实际输入或界面中修改。
- #215 CR Text Concatenate → `text2` ← #138 提示词。修改上游创作入口；若来源是自动分析/构建器，在其实际输入或界面中修改。
- #136 MiniMaxH3ReferenceToVideo → `prompt` ← #242 CR Text Concatenate。修改上游创作入口；若来源是自动分析/构建器，在其实际输入或界面中修改。

## 参数与运行

1. 在工作流列表打开 `光影处理/视频打光-文丨MiniMax.json`，确认名称和主要节点与本卡一致；缺节点时先处理加载问题。
2. 在上面列出的入口替换素材并查看预览；多图、多音色、首尾帧先绑定各自职责。
3. 按当前输入方式填写文本或操作时间线；无需提示词的工作流跳过文字生成。
4. 保留模型、采样器与内部系统模板；只修改本任务需要的尺寸、时长或强度。先做一张图或一小段音视频。
5. 点击 ComfyUI 的运行/队列按钮，等待输出；失败时读取第一个报错节点，缺文件先换有效素材，显存不足先降低尺寸或片段长度。

实际可调参数位置（以当前界面值为准，首次不要全部修改）：

- #124 BasicScheduler：`denoise`。
- #141 VHS_LoadVideo：`custom_height`、`custom_width`、`force_rate`、`frame_load_cap`、`select_every_nth`、`skip_first_frames`。

## 获取结果与验收

结果位置：#151 合并为视频。在对应预览保存/下载，核对本次输出时间。
对照处理前后的人脸、衣服和背景；视频逐段检查亮度跳变、光斑漂移。

失败重跑：保留原结果与种子记录，只调整一个明确变量；先判断是输入/节点错误还是画面、声音效果问题。

本卡依据工作流 JSON、节点公开字段和连线编写；尚未对本工作流执行 GPU 生成验收。用户实际 JSON 或界面变化时，以当前文件为准并重新核对。
