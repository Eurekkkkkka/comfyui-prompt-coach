# 视频补帧-GIMM-VFI

工作流路径：`视频补帧-GIMM-VFI.json`  
核对：2026-09-20｜以本卡实际节点为准｜无需创作提示词  
源文件 SHA-256：`6899ef0805b99ce5953285409024175de9f9aba68e9583e822280890818fb13e`

## 用途与准备

使用该独立工作流完成对应处理。
先按下面专用说明准备素材并确认当前模式。

## 本工作流先看

- #4 上传源视频；无需提示词。补帧设置与输出帧率一起核对，保持总时长不变；有切镜时检查插值鬼影。

## 素材放在哪里

| 位置 | 素材 | 说明 |
|---|---|---|
| #4 VHS_LoadVideo → `video` | 视频 | 实际连接标签：原始视频、视频信息；替换素材后核对预览 |

## 提示词和填写位置

按真实模型与节点填写；不以文件名推定提示词结构。

无需新写提示词。按上方素材入口和下方参数完成操作，不额外寻找正向/负向输入框。

## 参数与运行

1. 在工作流列表打开 `视频补帧-GIMM-VFI.json`，确认名称和主要节点与本卡一致；缺节点时先处理加载问题。
2. 在上面列出的入口替换素材并查看预览；多图、多音色、首尾帧先绑定各自职责。
3. 按当前输入方式填写文本或操作时间线；无需提示词的工作流跳过文字生成。
4. 保留模型、采样器与内部系统模板；只修改本任务需要的尺寸、时长或强度。先做一张图或一小段音视频。
5. 点击 ComfyUI 的运行/队列按钮，等待输出；失败时读取第一个报错节点，缺文件先换有效素材，显存不足先降低尺寸或片段长度。

实际可调参数位置（以当前界面值为准，首次不要全部修改）：

- #4 VHS_LoadVideo：`custom_height`、`custom_width`、`force_rate`、`frame_load_cap`、`select_every_nth`、`skip_first_frames`。

## 获取结果与验收

结果位置：#7 VHS_VideoCombine。在对应预览保存/下载，核对本次输出时间。
检查处理前后目标变化与需要保持的内容。

失败重跑：保留原结果与种子记录，只调整一个明确变量；先判断是输入/节点错误还是画面、声音效果问题。

本卡依据工作流 JSON、节点公开字段和连线编写；尚未对本工作流执行 GPU 生成验收。用户实际 JSON 或界面变化时，以当前文件为准并重新核对。
