# 图片超级放大-亿级像素-Z-image

工作流路径：`变清晰/图片超级放大-亿级像素-Z-image.json`  
核对：2026-09-20｜以本卡实际节点为准｜按字段填写  
源文件 SHA-256：`cfa8ca7916f4a47f78318f73103de3e7f735685473a22f98c11728776b059514`

## 用途与准备

修复细节或提高输出尺寸。
准备原图或源视频；明确目标尺寸与保真要求。文生图变体只需文字，不需要上传图片。

## 本工作流先看

- 按下方实际输入位置完成一次小规模试跑；首次保留模型、采样器和内部系统模板，效果问题只改一个变量再比较。

## 素材放在哪里

| 位置 | 素材 | 说明 |
|---|---|---|
| #54 LoadImage → `image` | 图片 | 实际连接标签：原始图像；替换素材后核对预览 |

## 提示词和填写位置

可填写的生成描述只补可见主体与纹理；别用新剧情改变原片。无提示词路线只调整真实参数。

| 实际输入位置 | 用途 | 怎么填 |
|---|---|---|
| #32 CLIPTextEncode → `text` | 正向/编辑指令 | 按用户目标填写；正文不含节点说明或尺寸参数 |

输入写法示例（用于说明表达方式；正式交付须替换为用户素材事实，并按本卡拆到对应字段）：

保留原图的主体、构图与颜色，恢复可见布料纹理和自然皮肤细节，不改变五官与画面文字。

## 参数与运行

1. 在工作流列表打开 `变清晰/图片超级放大-亿级像素-Z-image.json`，确认名称和主要节点与本卡一致；缺节点时先处理加载问题。
2. 在上面列出的入口替换素材并查看预览；多图、多音色、首尾帧先绑定各自职责。
3. 按当前输入方式填写文本或操作时间线；无需提示词的工作流跳过文字生成。
4. 保留模型、采样器与内部系统模板；只修改本任务需要的尺寸、时长或强度。先做一张图或一小段音视频。
5. 点击 ComfyUI 的运行/队列按钮，等待输出；失败时读取第一个报错节点，缺文件先换有效素材，显存不足先降低尺寸或片段长度。

实际可调参数位置（以当前界面值为准，首次不要全部修改）：

- #78 KSampler：`denoise`。
- #95 ImageScaleBy：`scale_by`。
- #101 ImageScaleBy：`scale_by`。

## 获取结果与验收

结果位置：#107 SaveImage。在对应预览保存/下载，核对本次输出时间。
核对实际宽高、肤质、文字、边缘与视频闪烁；输出尺寸以文件属性为准。

失败重跑：保留原结果与种子记录，只调整一个明确变量；先判断是输入/节点错误还是画面、声音效果问题。

本卡依据工作流 JSON、节点公开字段和连线编写；尚未对本工作流执行 GPU 生成验收。用户实际 JSON 或界面变化时，以当前文件为准并重新核对。
