# 局部重绘-万物消除-Klein

工作流路径：`局部重绘/局部重绘-万物消除-Klein.json`  
核对：2026-09-20｜Flux / Kontext / Klein / Krea｜按字段填写  
源文件 SHA-256：`416024b2b603cb3af38dc0c8ed880a824bfddd8ebf956387c7fe694c68836a6a`

## 用途与准备

在指定区域修复、消除、改字或合成。
准备原图与局部区域，使用画布或遮罩选定范围；融图变体另需要放入的素材。

## 本工作流先看

- 当前固定消除指令指绿色区域：遮罩/画布标色应一致，#109.text 填消除要求。

## 素材放在哪里

| 位置 | 素材 | 说明 |
|---|---|---|
| #432 LoadImage → `image` | 图片 | 实际连接标签：遮罩、初始图像；替换素材后核对预览 |

## 提示词和填写位置

明确修改区域的目标内容和边界外保持项；文字必须逐字给出。自动反推路线不要覆盖内部编码框。

| 实际输入位置 | 用途 | 怎么填 |
|---|---|---|
| #109 CLIPTextEncode → `text` | 正向/编辑指令 | 按用户目标填写；正文不含节点说明或尺寸参数 |

输入写法示例（用于说明表达方式；正式交付须替换为用户素材事实，并按本卡拆到对应字段）：

把选定区域修复成与周围一致的自然材质，保留区域外的人物、物体和构图。

## 参数与运行

1. 在工作流列表打开 `局部重绘/局部重绘-万物消除-Klein.json`，确认名称和主要节点与本卡一致；缺节点时先处理加载问题。
2. 在上面列出的入口替换素材并查看预览；多图、多音色、首尾帧先绑定各自职责。
3. 需要局部处理时，在实际画布/遮罩编辑入口选区，查看遮罩预览后再继续。下列节点用于定位选区处理链，不要求逐个编辑。
4. 保留模型、采样器与内部系统模板；只修改本任务需要的尺寸、时长或强度。先做一张图或一小段音视频。
5. 点击 ComfyUI 的运行/队列按钮，等待输出；失败时读取第一个报错节点，缺文件先换有效素材，显存不足先降低尺寸或片段长度。

本卡没有从公开字段识别到需手动修改的数值入口；沿用当前预设，或在上方导演台/子图界面设置。

选区处理链定位：#323 Mask Fill Holes；#334 ImageAndMaskPreview。

## 获取结果与验收

结果位置：#427 SaveImage。在对应预览保存/下载，核对本次输出时间。
放大检查遮罩边缘、结构、文字和光影接缝，确认未影响区保持。

失败重跑：保留原结果与种子记录，只调整一个明确变量；先判断是输入/节点错误还是画面、声音效果问题。

本卡依据工作流 JSON、节点公开字段和连线编写；尚未对本工作流执行 GPU 生成验收。用户实际 JSON 或界面变化时，以当前文件为准并重新核对。
