# 图片换背景-仅文本-ICLight

工作流路径：`换背景/图片换背景-仅文本-ICLight.json`  
核对：2026-09-20｜以本卡实际节点为准｜按字段填写  
源文件 SHA-256：`744a0bb5d6a8047c28615efde3df0678471cbfe0f59387d9281b0bcc8e3051fb`

## 用途与准备

保留前景人物，把环境换成目标场景。
原图/源视频提供人物；仅文本版用文字描述新环境，参考图版另传背景图。

## 本工作流先看

- 按下方实际输入位置完成一次小规模试跑；首次保留模型、采样器和内部系统模板，效果问题只改一个变量再比较。

## 素材放在哪里

| 位置 | 素材 | 说明 |
|---|---|---|
| #11 LoadImage → `image` | 图片 | 实际连接标签：原始图像；替换素材后核对预览 |

## 提示词和填写位置

明确替换背景，保留人物身份、服饰、动作、机位与时间线；新环境的光线要与人匹配。

| 实际输入位置 | 用途 | 怎么填 |
|---|---|---|
| #7 CLIPTextEncode → `text` | 负向 | 首次保留当前负向；只为实际问题追加短约束 |
| #17 GroundingDinoSAMSegment (segment anything) → `prompt` | 文本（用途见专用说明） | 按用户目标填写；正文不含节点说明或尺寸参数 |
| #6 CLIPTextEncode → `text` | 正向/编辑指令 | 按用户目标填写；正文不含节点说明或尺寸参数 |

输入写法示例（用于说明表达方式；正式交付须替换为用户素材事实，并按本卡拆到对应字段）：

将源画面的背景替换为傍晚的海岸，保持人物身份、服装、动作和构图，匹配暖色侧光与自然接触阴影。

## 参数与运行

1. 在工作流列表打开 `换背景/图片换背景-仅文本-ICLight.json`，确认名称和主要节点与本卡一致；缺节点时先处理加载问题。
2. 在上面列出的入口替换素材并查看预览；多图、多音色、首尾帧先绑定各自职责。
3. 需要局部处理时，在实际画布/遮罩编辑入口选区，查看遮罩预览后再继续。下列节点用于定位选区处理链，不要求逐个编辑。
4. 保留模型、采样器与内部系统模板；只修改本任务需要的尺寸、时长或强度。先做一张图或一小段音视频。
5. 点击 ComfyUI 的运行/队列按钮，等待输出；失败时读取第一个报错节点，缺文件先换有效素材，显存不足先降低尺寸或片段长度。

实际可调参数位置（以当前界面值为准，首次不要全部修改）：

- #14 ICLightConditioning：`multiplier`。
- #3 KSampler：`denoise`。
- #75 easy hiresFix：`height`、`width`。

选区处理链定位：#36 ImpactGaussianBlurMask；#35 GrowMask；#37 Convert Masks to Images；#66 Image Blend by Mask。

## 获取结果与验收

结果位置：#85 SaveImage。在对应预览保存/下载，核对本次输出时间。
检查人物轮廓、头发边缘、前后景关系；视频检查背景跟随镜头变化。

失败重跑：保留原结果与种子记录，只调整一个明确变量；先判断是输入/节点错误还是画面、声音效果问题。

本卡依据工作流 JSON、节点公开字段和连线编写；尚未对本工作流执行 GPU 生成验收。用户实际 JSON 或界面变化时，以当前文件为准并重新核对。
