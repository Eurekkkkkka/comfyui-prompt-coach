# 视频4K修复-FlashVSR

工作流路径：`变清晰/视频4K修复-FlashVSR.json`  
核对：2026-09-20｜以本卡实际节点为准｜无需创作提示词  
源文件 SHA-256：`bfb6d0bff40aa56b8b1616611ca36bcece5c7dd61b2bd8f4e607a47c8ff115c7`

## 用途与准备

修复细节或提高输出尺寸。
准备原图或源视频；明确目标尺寸与保真要求。文生图变体只需文字，不需要上传图片。

## 本工作流先看

- 按下方实际输入位置完成一次小规模试跑；首次保留模型、采样器和内部系统模板，效果问题只改一个变量再比较。

## 素材放在哪里

| 位置 | 素材 | 说明 |
|---|---|---|
| #1 VHS_LoadVideo → `video` | 视频 | 替换示例素材并核对预览；多素材顺序按专用说明确认 |

## 提示词和填写位置

可填写的生成描述只补可见主体与纹理；别用新剧情改变原片。无提示词路线只调整真实参数。

无需新写提示词。按上方素材入口和下方参数完成操作，不额外寻找正向/负向输入框。

## 参数与运行

1. 在工作流列表打开 `变清晰/视频4K修复-FlashVSR.json`，确认名称和主要节点与本卡一致；缺节点时先处理加载问题。
2. 在上面列出的入口替换素材并查看预览；多图、多音色、首尾帧先绑定各自职责。
3. 按当前输入方式填写文本或操作时间线；无需提示词的工作流跳过文字生成。
4. 保留模型、采样器与内部系统模板；只修改本任务需要的尺寸、时长或强度。先做一张图或一小段音视频。
5. 点击 ComfyUI 的运行/队列按钮，等待输出；失败时读取第一个报错节点，缺文件先换有效素材，显存不足先降低尺寸或片段长度。

实际可调参数位置（以当前界面值为准，首次不要全部修改）：

- #49 VHS_VideoCombine：`frame_rate`。
- #1 VHS_LoadVideo：`custom_height`、`custom_width`、`force_rate`、`frame_load_cap`、`select_every_nth`、`skip_first_frames`。

## 获取结果与验收

结果位置：#49 VHS_VideoCombine。在对应预览保存/下载，核对本次输出时间。
核对实际宽高、肤质、文字、边缘与视频闪烁；输出尺寸以文件属性为准。

失败重跑：保留原结果与种子记录，只调整一个明确变量；先判断是输入/节点错误还是画面、声音效果问题。

本卡依据工作流 JSON、节点公开字段和连线编写；尚未对本工作流执行 GPU 生成验收。用户实际 JSON 或界面变化时，以当前文件为准并重新核对。
