# 文生图-画布丨ideogram4

工作流路径：`文生图/文生图-画布丨ideogram4.json`  
核对：2026-09-20｜Ideogram｜按字段填写  
源文件 SHA-256：`ebc77c03994a23b4a04af528395c36ec993b29423227c717cca835028fb80618`

## 用途与准备

从文字生成图片。
说明主体、动作、场景、构图、光线、材质和所需文字。除可选参考分支外无需素材图。

## 本工作流先看

- #226“基础提示词”.text 写总画面，#225.text 写背景；它们进入 #224 构建器。
- 在 #224 画布确认各对象框的位置和描述、文字原文与层级。只改总提示词却留着旧示例画布，会把旧物体带入新图；同步修改或清空不需要的区域内容。

## 素材放在哪里

此快照没有外部生成素材上传入口；用文本或当前节点内部配置运行。

## 提示词和填写位置

用可观察的自然语言，画面文字加引号原样保留；尺寸、种子、采样参数在界面单独设置。

| 实际输入位置 | 用途 | 怎么填 |
|---|---|---|
| #224 Ideogram4PromptBuilderKJ → `aesthetics` | 正向/编辑指令 | 按用户目标填写；正文不含节点说明或尺寸参数 |
| #224 Ideogram4PromptBuilderKJ → `lighting` | 正向/编辑指令 | 按用户目标填写；正文不含节点说明或尺寸参数 |
| #224 Ideogram4PromptBuilderKJ → `medium` | 正向/编辑指令 | 按用户目标填写；正文不含节点说明或尺寸参数 |
| #226 基础提示词 → `text` | 正向/编辑指令 | 按用户目标填写；正文不含节点说明或尺寸参数 |
| #225 描述背景 → `text` | 正向/编辑指令 | 按用户目标填写；正文不含节点说明或尺寸参数 |

已接线的文本框由上游提供，直接改内部旧值可能不生效：

- #196 StringReplace → `string` ← #195 JsonExtractString。修改上游创作入口；若来源是自动分析/构建器，在其实际输入或界面中修改。
- #186 CLIPTextEncode → `text` ← #224 Ideogram4PromptBuilderKJ。修改上游创作入口；若来源是自动分析/构建器，在其实际输入或界面中修改。
- #224 Ideogram4PromptBuilderKJ → `high_level_description` ← #226 基础提示词。修改上游创作入口；若来源是自动分析/构建器，在其实际输入或界面中修改。
- #224 Ideogram4PromptBuilderKJ → `background` ← #225 描述背景。修改上游创作入口；若来源是自动分析/构建器，在其实际输入或界面中修改。

输入写法示例（用于说明表达方式；正式交付须替换为用户素材事实，并按本卡拆到对应字段）：

一只白色陶瓷茶杯放在木桌上，旁边是一枝杏花，侧面窗光照亮杯沿，近景构图，背景安静简洁。

## 参数与运行

1. 在工作流列表打开 `文生图/文生图-画布丨ideogram4.json`，确认名称和主要节点与本卡一致；缺节点时先处理加载问题。
2. 在上面列出的入口替换素材并查看预览；多图、多音色、首尾帧先绑定各自职责。
3. 按当前输入方式填写文本或操作时间线；无需提示词的工作流跳过文字生成。
4. 保留模型、采样器与内部系统模板；只修改本任务需要的尺寸、时长或强度。先做一张图或一小段音视频。
5. 点击 ComfyUI 的运行/队列按钮，等待输出；失败时读取第一个报错节点，缺文件先换有效素材，显存不足先降低尺寸或片段长度。

本卡没有从公开字段识别到需手动修改的数值入口；沿用当前预设，或在上方导演台/子图界面设置。

## 获取结果与验收

结果位置：#291 SaveImage。在对应预览保存/下载，核对本次输出时间。
检查人物结构、构图、文字与风格；做资产时确认单图或人物视图要求。

失败重跑：保留原结果与种子记录，只调整一个明确变量；先判断是输入/节点错误还是画面、声音效果问题。

本卡依据工作流 JSON、节点公开字段和连线编写；尚未对本工作流执行 GPU 生成验收。用户实际 JSON 或界面变化时，以当前文件为准并重新核对。
