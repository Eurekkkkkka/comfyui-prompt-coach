# 视频对口型-InfiniteTalk

工作流路径：`对口型/视频对口型-InfiniteTalk.json`  
核对：2026-09-20｜Wan / Animate / VACE / InfiniteTalk｜按字段填写  
源文件 SHA-256：`2b35a01759901e60f7416bde08849b7c31a24e1835d779077731a5c6cd9206b6`

## 用途与准备

用音频驱动人物口型与表演。
按变体上传人物图或人物视频，再上传最终对白或演唱音频。

## 本工作流先看

- 按下方实际输入位置完成一次小规模试跑；首次保留模型、采样器和内部系统模板，效果问题只改一个变量再比较。

## 素材放在哪里

| 位置 | 素材 | 说明 |
|---|---|---|
| #206 VHS_LoadVideo → `video` | 视频 | 替换示例素材并核对预览；多素材顺序按专用说明确认 |
| #125 LoadAudio → `audio` | 音频 | 实际连接标签：音频；替换素材后核对预览 |

## 提示词和填写位置

只描述人物表演幅度、视线、镜头与背景保持；对白内容由音频决定，别再写另一套台词。

| 实际输入位置 | 用途 | 怎么填 |
|---|---|---|
| #135 WanVideoTextEncode → `positive_prompt` | 文本（用途见专用说明） | 按用户目标填写；正文不含节点说明或尺寸参数 |
| #135 WanVideoTextEncode → `negative_prompt` | 负向 | 首次保留当前负向；只为实际问题追加短约束 |

输入写法示例（用于说明表达方式；正式交付须替换为用户素材事实，并按本卡拆到对应字段）：

人物保持身份与服装一致，跟随输入音频自然对口型，头部与肩膀只做轻微动作，固定镜头，背景保持稳定。

## 参数与运行

1. 在工作流列表打开 `对口型/视频对口型-InfiniteTalk.json`，确认名称和主要节点与本卡一致；缺节点时先处理加载问题。
2. 在上面列出的入口替换素材并查看预览；多图、多音色、首尾帧先绑定各自职责。
3. 按当前输入方式填写文本或操作时间线；无需提示词的工作流跳过文字生成。
4. 保留模型、采样器与内部系统模板；只修改本任务需要的尺寸、时长或强度。先做一张图或一小段音视频。
5. 点击 ComfyUI 的运行/队列按钮，等待输出；失败时读取第一个报错节点，缺文件先换有效素材，显存不足先降低尺寸或片段长度。

实际可调参数位置（以当前界面值为准，首次不要全部修改）：

- #194 MultiTalkWav2VecEmbeds：`fps`。
- #206 VHS_LoadVideo：`custom_height`、`custom_width`、`force_rate`、`frame_load_cap`、`select_every_nth`、`skip_first_frames`。
- #207 ImageResizeKJv2：`height`、`width`。
- #131 VHS_VideoCombine：`frame_rate`。

## 获取结果与验收

结果位置：#131 VHS_VideoCombine。在对应预览保存/下载，核对本次输出时间。
试听并逐段检查口型、停顿、齿部、脸部稳定性与画面音频时长。

失败重跑：保留原结果与种子记录，只调整一个明确变量；先判断是输入/节点错误还是画面、声音效果问题。

本卡依据工作流 JSON、节点公开字段和连线编写；尚未对本工作流执行 GPU 生成验收。用户实际 JSON 或界面变化时，以当前文件为准并重新核对。
