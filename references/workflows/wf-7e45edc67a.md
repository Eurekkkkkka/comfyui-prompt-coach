# 局部重绘-透视融图-Kontext

工作流路径：`换物/局部重绘-透视融图-Kontext.json`  
核对：2026-09-20｜Flux / Kontext / Klein / Krea｜按字段填写  
源文件 SHA-256：`b7ef2e25e04469a3e5545bb3ac05a8111c104b0d6acc30f66c748787da91244d`

## 用途与准备

将参考物体置入目标场景或替换局部物品。
分别提供物体素材与目标底图，确认遮罩、摆放位置和比例。

## 本工作流先看

- #287 PainterFluxImageEdit.prompt 是融图要求，#13.text 是后续整合描述；两者必须一致。#257 与 #362 分别提供素材，先从画布预览确认前景与底图职责。
- 先在画布调整位置、比例、透视再生成，提示词不能代替几何摆放。

## 素材放在哪里

| 位置 | 素材 | 说明 |
|---|---|---|
| #257 LoadImage → `image` | 图片 | 实际连接标签：主体图；替换素材后核对预览 |
| #362 LoadImage → `image` | 图片 | 实际连接标签：背景图；替换素材后核对预览 |

## 提示词和填写位置

写清哪张图提供物体、哪张提供场景，以及物体尺度、透视与接触阴影。

| 实际输入位置 | 用途 | 怎么填 |
|---|---|---|
| #287 PainterFluxImageEdit → `prompt` | 文本（用途见专用说明） | 按用户目标填写；正文不含节点说明或尺寸参数 |
| #13 CLIPTextEncode → `text` | 正向/编辑指令 | 按用户目标填写；正文不含节点说明或尺寸参数 |

输入写法示例（用于说明表达方式；正式交付须替换为用户素材事实，并按本卡拆到对应字段）：

将参考物体放置在底图指定区域，匹配尺度、透视、光线和接触阴影，保持底图其余部分不变。正式文本必须注明每张图的职责。

## 参数与运行

1. 在工作流列表打开 `换物/局部重绘-透视融图-Kontext.json`，确认名称和主要节点与本卡一致；缺节点时先处理加载问题。
2. 在上面列出的入口替换素材并查看预览；多图、多音色、首尾帧先绑定各自职责。
3. 需要局部处理时，在实际画布/遮罩编辑入口选区，查看遮罩预览后再继续。下列节点用于定位选区处理链，不要求逐个编辑。
4. 保留模型、采样器与内部系统模板；只修改本任务需要的尺寸、时长或强度。先做一张图或一小段音视频。
5. 点击 ComfyUI 的运行/队列按钮，等待输出；失败时读取第一个报错节点，缺文件先换有效素材，显存不足先降低尺寸或片段长度。

实际可调参数位置（以当前界面值为准，首次不要全部修改）：

- #61 KSampler：`denoise`。
- #289 KSampler：`denoise`。

选区处理链定位：#291 SolidMask。

## 获取结果与验收

结果位置：#363 SaveImage。在对应预览保存/下载，核对本次输出时间。
检查物体结构、位置、遮挡关系、光照方向与场景保持。

失败重跑：保留原结果与种子记录，只调整一个明确变量；先判断是输入/节点错误还是画面、声音效果问题。

本卡依据工作流 JSON、节点公开字段和连线编写；尚未对本工作流执行 GPU 生成验收。用户实际 JSON 或界面变化时，以当前文件为准并重新核对。
