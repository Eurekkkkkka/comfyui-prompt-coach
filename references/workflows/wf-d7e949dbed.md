# MiniMax Music 3-W4A8（官方Caption重写V2）

工作流路径：`声音处理/MiniMax Music 3-W4A8（官方Caption重写V2）.json`  
核对：2026-09-20｜Music 3｜按字段填写  
源文件 SHA-256：`dc992890df1e01ea6f618b05dda4a4ac0bb22ebbafed5915671600831340ce48`

## 用途与准备

生成或处理台词、音色、配乐或响度。
按本卡准备文本、源音频或参考音色；参考音频尽量单人、干净、无背景音乐。

## 本工作流先看

- #41.brief 写曲风、乐器、速度、唱法与动态；lyrics 写原歌词或留空做纯音乐；constraints 写时长目标等音乐限制。
- 普通需求沿用 Rewrite；已有符合节点要求的完整 Caption 才考虑 Passthrough。H3 子技能不适用于 Music 3。

## 素材放在哪里

此快照没有外部生成素材上传入口；用文本或当前节点内部配置运行。

## 提示词和填写位置

台词写实际要说的句子；音色、曲风和歌词分开填。变声、响度处理没有台词输入时不要虚构。

| 实际输入位置 | 用途 | 怎么填 |
|---|---|---|
| #41 MiniMaxMusic3CaptionRewriter → `brief` | 正向/编辑指令 | 按用户目标填写；正文不含节点说明或尺寸参数 |
| #41 MiniMaxMusic3CaptionRewriter → `lyrics` | 歌词 | 按用户目标填写；正文不含节点说明或尺寸参数 |
| #41 MiniMaxMusic3CaptionRewriter → `constraints` | 约束 | 填写保持项和禁止项；不与正文冲突 |

已接线的文本框由上游提供，直接改内部旧值可能不生效：

- #44 SaveMiniMaxMusic3Caption → `lyrics` ← #41 MiniMaxMusic3CaptionRewriter。修改上游创作入口；若来源是自动分析/构建器，在其实际输入或界面中修改。
- #49 MiniMaxMusic3TextEncode → `lyrics` ← #41 MiniMaxMusic3CaptionRewriter。修改上游创作入口；若来源是自动分析/构建器，在其实际输入或界面中修改。

输入写法示例（用于说明表达方式；正式交付须替换为用户素材事实，并按本卡拆到对应字段）：

brief 示例：温暖的木吉他流行曲，中速节拍，轻柔女声，主歌克制，副歌逐渐加入鼓和贝斯。歌词另外填 lyrics。

## 参数与运行

1. 在工作流列表打开 `声音处理/MiniMax Music 3-W4A8（官方Caption重写V2）.json`，确认名称和主要节点与本卡一致；缺节点时先处理加载问题。
2. 在上面列出的入口替换素材并查看预览；多图、多音色、首尾帧先绑定各自职责。
3. 按当前输入方式填写文本或操作时间线；无需提示词的工作流跳过文字生成。
4. 保留模型、采样器与内部系统模板；只修改本任务需要的尺寸、时长或强度。先做一张图或一小段音视频。
5. 点击 ComfyUI 的运行/队列按钮，等待输出；失败时读取第一个报错节点，缺文件先换有效素材，显存不足先降低尺寸或片段长度。

实际可调参数位置（以当前界面值为准，首次不要全部修改）：

- #54 KSampler：`denoise`。

## 获取结果与验收

结果位置：#44 SaveMiniMaxMusic3Caption；#35 SaveAudioAdvanced。在对应预览保存/下载，核对本次输出时间。
完整试听，检查漏字、重复、说话人、口音、爆音和尾音截断。

失败重跑：保留原结果与种子记录，只调整一个明确变量；先判断是输入/节点错误还是画面、声音效果问题。

本卡依据工作流 JSON、节点公开字段和连线编写；尚未对本工作流执行 GPU 生成验收。用户实际 JSON 或界面变化时，以当前文件为准并重新核对。
