# 皮肤纹理修复-图片-SUPIR

工作流路径：`皮肤纹理修复-图片-SUPIR.json`  
核对：2026-09-20｜以本卡实际节点为准｜按字段填写  
源文件 SHA-256：`afed215f6e67d71531a2dcf4fdf9e1bbaff31b36d16130c6633b90209bd5190b`

## 用途与准备

使用该独立工作流完成对应处理。
先按下面专用说明准备素材并确认当前模式。

## 本工作流先看

- #29 上传人物图，目标是恢复自然纹理；避免用磨皮或锐化词制造蜡像感。检查皮肤、眼睛和轮廓是否保真。

## 素材放在哪里

| 位置 | 素材 | 说明 |
|---|---|---|
| #29 LoadImage → `image` | 图片 | 实际连接标签：原始图像；替换素材后核对预览 |

## 提示词和填写位置

按真实模型与节点填写；不以文件名推定提示词结构。

| 实际输入位置 | 用途 | 怎么填 |
|---|---|---|
| #12 SUPIR_conditioner → `positive_prompt` | 文本（用途见专用说明） | 按用户目标填写；正文不含节点说明或尺寸参数 |
| #12 SUPIR_conditioner → `negative_prompt` | 负向 | 首次保留当前负向；只为实际问题追加短约束 |
| #35 CLIPTextEncode → `text` | 文本（用途见专用说明） | 按用户目标填写；正文不含节点说明或尺寸参数 |
| #36 CLIPTextEncode → `text` | 文本（用途见专用说明） | 按用户目标填写；正文不含节点说明或尺寸参数 |

输入写法示例（用于说明表达方式；正式交付须替换为用户素材事实，并按本卡拆到对应字段）：

保留原图的主体、构图与颜色，恢复可见布料纹理和自然皮肤细节，不改变五官与画面文字。

## 参数与运行

1. 在工作流列表打开 `皮肤纹理修复-图片-SUPIR.json`，确认名称和主要节点与本卡一致；缺节点时先处理加载问题。
2. 在上面列出的入口替换素材并查看预览；多图、多音色、首尾帧先绑定各自职责。
3. 按当前输入方式填写文本或操作时间线；无需提示词的工作流跳过文字生成。
4. 保留模型、采样器与内部系统模板；只修改本任务需要的尺寸、时长或强度。先做一张图或一小段音视频。
5. 点击 ComfyUI 的运行/队列按钮，等待输出；失败时读取第一个报错节点，缺文件先换有效素材，显存不足先降低尺寸或片段长度。

实际可调参数位置（以当前界面值为准，首次不要全部修改）：

- #24 ImageScaleBy：`scale_by`。
- #103 ImageScaleBy：`scale_by`。
- #79 KSampler：`denoise`。
- #126 ModelSamplingFlux：`height`、`width`。
- #135 UltimateSDUpscale：`denoise`。

## 获取结果与验收

结果位置：#139 SaveImage。在对应预览保存/下载，核对本次输出时间。
检查处理前后目标变化与需要保持的内容。

失败重跑：保留原结果与种子记录，只调整一个明确变量；先判断是输入/节点错误还是画面、声音效果问题。

本卡依据工作流 JSON、节点公开字段和连线编写；尚未对本工作流执行 GPU 生成验收。用户实际 JSON 或界面变化时，以当前文件为准并重新核对。
