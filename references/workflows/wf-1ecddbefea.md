# 图片换脸-turbo

工作流路径：`换脸/图片换脸-turbo.json`  
核对：2026-09-20｜以本卡实际节点为准｜按字段填写  
源文件 SHA-256：`96595bf00e1f8dd3ad6512d87a6a74b75dd3aa161549d92ebc73a37f6319efae`

## 用途与准备

按参考素材替换脸部特征。
先辨明承载姿势的底图与提供脸部特征的参考图；保持清晰与相近视角。

## 本工作流先看

- 按下方实际输入位置完成一次小规模试跑；首次保留模型、采样器和内部系统模板，效果问题只改一个变量再比较。

## 素材放在哪里

| 位置 | 素材 | 说明 |
|---|---|---|
| #238 LoadImage → `image` | 图片 | 实际连接标签：原模特；替换素材后核对预览 |
| #244 LoadImage → `image` | 图片 | 实际连接标签：替换素材；替换素材后核对预览 |

## 提示词和填写位置

写清仅替换头脸特征，保持原图服装、身体、姿态、背景和光影。

| 实际输入位置 | 用途 | 怎么填 |
|---|---|---|
| #39 CLIPTextEncode → `text` | 正向/编辑指令 | 按用户目标填写；正文不含节点说明或尺寸参数 |
| #40 CLIPTextEncode → `text` | 负向 | 首次保留当前负向；只为实际问题追加短约束 |
| #154 CLIPTextEncode → `text` | 负向 | 首次保留当前负向；只为实际问题追加短约束 |
| #153 CLIPTextEncode → `text` | 正向/编辑指令 | 按用户目标填写；正文不含节点说明或尺寸参数 |

输入写法示例（用于说明表达方式；正式交付须替换为用户素材事实，并按本卡拆到对应字段）：

将底图人物的头脸特征替换为参考人物的头脸特征，保持底图身体、衣服、姿势、背景和光线不变。

## 参数与运行

1. 在工作流列表打开 `换脸/图片换脸-turbo.json`，确认名称和主要节点与本卡一致；缺节点时先处理加载问题。
2. 在上面列出的入口替换素材并查看预览；多图、多音色、首尾帧先绑定各自职责。
3. 按当前输入方式填写文本或操作时间线；无需提示词的工作流跳过文字生成。
4. 保留模型、采样器与内部系统模板；只修改本任务需要的尺寸、时长或强度。先做一张图或一小段音视频。
5. 点击 ComfyUI 的运行/队列按钮，等待输出；失败时读取第一个报错节点，缺文件先换有效素材，显存不足先降低尺寸或片段长度。

实际可调参数位置（以当前界面值为准，首次不要全部修改）：

- #79 DZ_Face_Detailer：`denoise`。
- #150 ImageScaleBy：`scale_by`。
- #159 KSampler：`denoise`。
- #185 FaceDetailer：`denoise`。

## 获取结果与验收

结果位置：#241 SaveImage。在对应预览保存/下载，核对本次输出时间。
检查脸型、发际线、颈部衔接、眼神和目标身份是否一致。

失败重跑：保留原结果与种子记录，只调整一个明确变量；先判断是输入/节点错误还是画面、声音效果问题。

本卡依据工作流 JSON、节点公开字段和连线编写；尚未对本工作流执行 GPU 生成验收。用户实际 JSON 或界面变化时，以当前文件为准并重新核对。
