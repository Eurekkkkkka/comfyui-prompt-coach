# 一键抠图-图片

工作流路径：`抠图/一键抠图-图片.json`  
核对：2026-09-20｜以本卡实际节点为准｜无需创作提示词  
源文件 SHA-256：`1f5fa4ac8b3351e13861f8ac0bc62f0aa24fb5bfc9e22a1399364b9c081120b7`

## 用途与准备

提取前景并输出透明背景图。
上传边缘清楚的原图，先预览分割结果。

## 本工作流先看

- 按下方实际输入位置完成一次小规模试跑；首次保留模型、采样器和内部系统模板，效果问题只改一个变量再比较。

## 素材放在哪里

| 位置 | 素材 | 说明 |
|---|---|---|
| #57 LoadImage → `image` | 图片 | 实际连接标签：原始图像；替换素材后核对预览 |

## 提示词和填写位置

本路线不需要创作提示词；关注实际分割与边缘处理。

无需新写提示词。按上方素材入口和下方参数完成操作，不额外寻找正向/负向输入框。

## 参数与运行

1. 在工作流列表打开 `抠图/一键抠图-图片.json`，确认名称和主要节点与本卡一致；缺节点时先处理加载问题。
2. 在上面列出的入口替换素材并查看预览；多图、多音色、首尾帧先绑定各自职责。
3. 按当前输入方式填写文本或操作时间线；无需提示词的工作流跳过文字生成。
4. 保留模型、采样器与内部系统模板；只修改本任务需要的尺寸、时长或强度。先做一张图或一小段音视频。
5. 点击 ComfyUI 的运行/队列按钮，等待输出；失败时读取第一个报错节点，缺文件先换有效素材，显存不足先降低尺寸或片段长度。

本卡没有从公开字段识别到需手动修改的数值入口；沿用当前预设，或在上方导演台/子图界面设置。

## 获取结果与验收

结果位置：#6 SaveImage；#67 SaveImage；#73 SaveImage；#80 SaveImage；#93 SaveImage；#98 SaveImage。在对应预览保存/下载，核对本次输出时间。
使用支持透明度的输出检查 alpha，检查发丝、半透明材质与边缘白边。

失败重跑：保留原结果与种子记录，只调整一个明确变量；先判断是输入/节点错误还是画面、声音效果问题。

本卡依据工作流 JSON、节点公开字段和连线编写；尚未对本工作流执行 GPU 生成验收。用户实际 JSON 或界面变化时，以当前文件为准并重新核对。
