# 角色资产四视图丨Krea2

工作流路径：`多视角分镜/角色资产四视图丨Krea2.json`  
核对：2026-09-20｜Flux / Kontext / Klein / Krea｜无需创作提示词  
源文件 SHA-256：`558ed2738cb93db16d025c5267fb6ace0ce3d091318463f9f0f1e221ac4c92a9`

## 用途与准备

用同一角色或场景生成视角变化或资产视图。
提供主视觉或人物图；先列出需要的视角，保持身份与服装版本一致。

## 本工作流先看

- 无需新写提示词：#41 上传人物，#24 内置四视图指令；#22 负向保持空白。
- 输出设计为脸部特写、正面全身、侧面全身、背面全身，部分全身视图的脸被灰块遮住是模板行为，不是生成事故。此工作流不能当作普通场景/道具单图，也不等于课程人物三视图。

## 素材放在哪里

| 位置 | 素材 | 说明 |
|---|---|---|
| #41 LoadImage → `image` | 图片 | 实际连接标签：初始图片；替换素材后核对预览 |

## 提示词和填写位置

视角变换与故事动作分开；写清摄像机移动方向和最终景别。固定四视图不需要新提示词。

无需新写提示词。按上方素材入口和下方参数完成操作，不额外寻找正向/负向输入框。

## 参数与运行

1. 在工作流列表打开 `多视角分镜/角色资产四视图丨Krea2.json`，确认名称和主要节点与本卡一致；缺节点时先处理加载问题。
2. 在上面列出的入口替换素材并查看预览；多图、多音色、首尾帧先绑定各自职责。
3. 按当前输入方式填写文本或操作时间线；无需提示词的工作流跳过文字生成。
4. 保留模型、采样器与内部系统模板；只修改本任务需要的尺寸、时长或强度。先做一张图或一小段音视频。
5. 点击 ComfyUI 的运行/队列按钮，等待输出；失败时读取第一个报错节点，缺文件先换有效素材，显存不足先降低尺寸或片段长度。

实际可调参数位置（以当前界面值为准，首次不要全部修改）：

- #18 ImageResizeKJv2：`height`、`width`。
- #7 EmptySD3LatentImage：`height`、`width`。
- #14 KSampler：`denoise`。

## 获取结果与验收

结果位置：#5 SaveImage。在对应预览保存/下载，核对本次输出时间。
核对角色脸、衣服、道具一致，分清不同视角与重复同一图；场景不要变成拼贴。

失败重跑：保留原结果与种子记录，只调整一个明确变量；先判断是输入/节点错误还是画面、声音效果问题。

本卡依据工作流 JSON、节点公开字段和连线编写；尚未对本工作流执行 GPU 生成验收。用户实际 JSON 或界面变化时，以当前文件为准并重新核对。
