# 图片换装-手动遮罩-Edit2511

工作流路径：`换装/图片换装-手动遮罩-Edit2511.json`  
核对：2026-09-20｜Qwen / Edit｜按字段填写  
源文件 SHA-256：`f14a543247829dec23202bc3da0dd62e46ae55f87af52fa44bc6898e40df2443`

## 用途与准备

把指定服装属性应用到目标人物。
上传人物与服装参考；手动版还需检查姿势图/遮罩，自动版先预览分割区域。

## 本工作流先看

- 该变体有三个图片入口：#609 提供服装，#612 提供模特身份与遮罩，#607 提供姿势。#372.prompt 预设语义为 image1 服装、image2 面部/发型、image3 姿势；不能把两图自动版的顺序硬套过来。
- #612 默认是带遮罩素材；替换后重新核对遮罩，不沿用旧人物的遮罩。

## 素材放在哪里

| 位置 | 素材 | 说明 |
|---|---|---|
| #609 LoadImage → `image` | 图片 | 实际连接标签：服装图像；替换素材后核对预览 |
| #607 LoadImage → `image` | 图片 | 实际连接标签：服装图像_0、姿势图像；替换素材后核对预览 |
| #612 LoadImage → `image` | 图片 | 实际连接标签：模特遮罩、模特图像；替换素材后核对预览 |

## 提示词和填写位置

按本工作流图序写明服装、身份与姿势来源；不把别的换装模板图序直接照搬。

| 实际输入位置 | 用途 | 怎么填 |
|---|---|---|
| #372 TextEncodeQwenImageEditPlusCustom_lrzjason → `prompt` | 正向/编辑指令 | 按用户目标填写；正文不含节点说明或尺寸参数 |

输入写法示例（用于说明表达方式；正式交付须替换为用户素材事实，并按本卡拆到对应字段）：

将目标服装的剪裁、颜色、面料与纹样应用到人物身上，保持人物身份、姿势、发型和背景不变。生成正式文本时补上本卡实际图序。

## 参数与运行

1. 在工作流列表打开 `换装/图片换装-手动遮罩-Edit2511.json`，确认名称和主要节点与本卡一致；缺节点时先处理加载问题。
2. 在上面列出的入口替换素材并查看预览；多图、多音色、首尾帧先绑定各自职责。
3. 需要局部处理时，在实际画布/遮罩编辑入口选区，查看遮罩预览后再继续。下列节点用于定位选区处理链，不要求逐个编辑。
4. 保留模型、采样器与内部系统模板；只修改本任务需要的尺寸、时长或强度。先做一张图或一小段音视频。
5. 点击 ComfyUI 的运行/队列按钮，等待输出；失败时读取第一个报错节点，缺文件先换有效素材，显存不足先降低尺寸或片段长度。

实际可调参数位置（以当前界面值为准，首次不要全部修改）：

- #427 KSampler：`denoise`。

选区处理链定位：#540 ImageAndMaskResizeNode；#578 ImageAndMaskPreview。

## 获取结果与验收

结果位置：#603 SaveImage。在对应预览保存/下载，核对本次输出时间。
检查衣服花纹、领口、手部遮挡、人体比例与脸部身份。

失败重跑：保留原结果与种子记录，只调整一个明确变量；先判断是输入/节点错误还是画面、声音效果问题。

本卡依据工作流 JSON、节点公开字段和连线编写；尚未对本工作流执行 GPU 生成验收。用户实际 JSON 或界面变化时，以当前文件为准并重新核对。
