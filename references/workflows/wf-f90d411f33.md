# 图片换装-自动遮罩-Edit2511

工作流路径：`换装/图片换装-自动遮罩-Edit2511.json`  
核对：2026-09-20｜Qwen / Edit｜按字段填写  
源文件 SHA-256：`60221b82b9d64a52da6def6ab3742d532ae0eaf89bb213d62c53e04654e4e167`

## 用途与准备

把指定服装属性应用到目标人物。
上传人物与服装参考；手动版还需检查姿势图/遮罩，自动版先预览分割区域。

## 本工作流先看

- #16.prompt 填图1人物穿图2服装；#6 是送入 ConditioningZeroOut 的空条件，保持空白。先检查自动服装遮罩，再执行换装。
- 自动识别后先看遮罩/预览；误选时先修正目标或换手动变体，提示词不能修复错误遮罩。

## 素材放在哪里

| 位置 | 素材 | 说明 |
|---|---|---|
| #74 LoadImage → `image` | 图片 | 实际连接标签：模特图像；替换素材后核对预览 |
| #71 LoadImage → `image` | 图片 | 实际连接标签：服装图像；替换素材后核对预览 |

## 提示词和填写位置

按本工作流图序写明服装、身份与姿势来源；不把别的换装模板图序直接照搬。

| 实际输入位置 | 用途 | 怎么填 |
|---|---|---|
| #6 TextEncodeQwenImageEditPlus → `prompt` | 负向 | 空条件送入 ConditioningZeroOut，保持空白 |
| #16 TextEncodeQwenImageEditPlus (Positive) → `prompt` | 正向/编辑指令 | 按用户目标填写；正文不含节点说明或尺寸参数 |

输入写法示例（用于说明表达方式；正式交付须替换为用户素材事实，并按本卡拆到对应字段）：

将目标服装的剪裁、颜色、面料与纹样应用到人物身上，保持人物身份、姿势、发型和背景不变。生成正式文本时补上本卡实际图序。

## 参数与运行

1. 在工作流列表打开 `换装/图片换装-自动遮罩-Edit2511.json`，确认名称和主要节点与本卡一致；缺节点时先处理加载问题。
2. 在上面列出的入口替换素材并查看预览；多图、多音色、首尾帧先绑定各自职责。
3. 按当前输入方式填写文本或操作时间线；无需提示词的工作流跳过文字生成。
4. 保留模型、采样器与内部系统模板；只修改本任务需要的尺寸、时长或强度。先做一张图或一小段音视频。
5. 点击 ComfyUI 的运行/队列按钮，等待输出；失败时读取第一个报错节点，缺文件先换有效素材，显存不足先降低尺寸或片段长度。

实际可调参数位置（以当前界面值为准，首次不要全部修改）：

- #11 KSampler：`denoise`。

## 获取结果与验收

结果位置：#80 SaveImage。在对应预览保存/下载，核对本次输出时间。
检查衣服花纹、领口、手部遮挡、人体比例与脸部身份。

失败重跑：保留原结果与种子记录，只调整一个明确变量；先判断是输入/节点错误还是画面、声音效果问题。

本卡依据工作流 JSON、节点公开字段和连线编写；尚未对本工作流执行 GPU 生成验收。用户实际 JSON 或界面变化时，以当前文件为准并重新核对。
