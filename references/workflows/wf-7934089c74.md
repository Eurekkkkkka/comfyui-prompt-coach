# 配音二创-AudioDiT

工作流路径：`声音处理/配音二创-AudioDiT.json`  
核对：2026-09-20｜语音｜按字段填写  
源文件 SHA-256：`fa4b16e0a27ace9ac9b1e1426a0807a38217faf2c8ece2044a1e6d0c1ceb6e09`

## 用途与准备

生成或处理台词、音色、配乐或响度。
按本卡准备文本、源音频或参考音色；参考音频尽量单人、干净、无背景音乐。

## 本工作流先看

- #94 上传参考音频，#9 CR Prompt Text.prompt 填新台词。对白文字与参考音色分开；用短句先检查发音与停顿。

## 素材放在哪里

| 位置 | 素材 | 说明 |
|---|---|---|
| #94 LoadAudio → `audio` | 音频 | 实际连接标签：原始音频；替换素材后核对预览 |

## 提示词和填写位置

台词写实际要说的句子；音色、曲风和歌词分开填。变声、响度处理没有台词输入时不要虚构。

| 实际输入位置 | 用途 | 怎么填 |
|---|---|---|
| #9 CR Prompt Text → `prompt` | 文本（用途见专用说明） | 按用户目标填写；正文不含节点说明或尺寸参数 |

已接线的文本框由上游提供，直接改内部旧值可能不生效：

- #12 LongCatVoiceCloneTTS → `text` ← #9 CR Prompt Text。修改上游创作入口；若来源是自动分析/构建器，在其实际输入或界面中修改。

输入写法示例（用于说明表达方式；正式交付须替换为用户素材事实，并按本卡拆到对应字段）：

台词字段示例：今天，我们继续完成这段故事。音色说明仅填专用音色字段；双人版另按 [S1]、[S2] 标记。

## 参数与运行

1. 在工作流列表打开 `声音处理/配音二创-AudioDiT.json`，确认名称和主要节点与本卡一致；缺节点时先处理加载问题。
2. 在上面列出的入口替换素材并查看预览；多图、多音色、首尾帧先绑定各自职责。
3. 按当前输入方式填写文本或操作时间线；无需提示词的工作流跳过文字生成。
4. 保留模型、采样器与内部系统模板；只修改本任务需要的尺寸、时长或强度。先做一张图或一小段音视频。
5. 点击 ComfyUI 的运行/队列按钮，等待输出；失败时读取第一个报错节点，缺文件先换有效素材，显存不足先降低尺寸或片段长度。

本卡没有从公开字段识别到需手动修改的数值入口；沿用当前预设，或在上方导演台/子图界面设置。

## 获取结果与验收

结果位置：#119 SaveAudioMP3；#11 SaveAudioMP3。在对应预览保存/下载，核对本次输出时间。
完整试听，检查漏字、重复、说话人、口音、爆音和尾音截断。

失败重跑：保留原结果与种子记录，只调整一个明确变量；先判断是输入/节点错误还是画面、声音效果问题。

本卡依据工作流 JSON、节点公开字段和连线编写；尚未对本工作流执行 GPU 生成验收。用户实际 JSON 或界面变化时，以当前文件为准并重新核对。
