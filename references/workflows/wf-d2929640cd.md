# 光影重塑-图片-Edit2509

工作流路径：`光影处理/光影重塑-图片-Edit2509.json`  
核对：2026-09-20｜Qwen / Edit｜按字段填写  
源文件 SHA-256：`67d272677b187f0499c22f6e8f44a99f88eeb13c532bcfebc3c41765f8c66708`

## 用途与准备

改变图片或视频的布光，保留主体与构图。
图片提供原图；视频提供源片。明确光源方位、软硬、色温与需要保留的人物。

## 本工作流先看

- 按下方实际输入位置完成一次小规模试跑；首次保留模型、采样器和内部系统模板，效果问题只改一个变量再比较。

## 素材放在哪里

| 位置 | 素材 | 说明 |
|---|---|---|
| #31 LoadImage → `image` | 图片 | 实际连接标签：原始图像；替换素材后核对预览 |

## 提示词和填写位置

写清主光来自哪里、照到哪里、阴影落在哪里；视频要求光照随动作连续，不新增物体。

| 实际输入位置 | 用途 | 怎么填 |
|---|---|---|
| #3 TextEncodeQwenImageEditPlus → `prompt` | 负向 | 首次保留当前负向；只为实际问题追加短约束 |
| #11 TextEncodeQwenImageEditPlus → `prompt` | 正向/编辑指令 | 按用户目标填写；正文不含节点说明或尺寸参数 |

输入写法示例（用于说明表达方式；正式交付须替换为用户素材事实，并按本卡拆到对应字段）：

将主光改为人物左前方的柔和暖光，阴影自然落向右后侧；保持人物身份、服装、构图与环境物体不变。

## 参数与运行

1. 在工作流列表打开 `光影处理/光影重塑-图片-Edit2509.json`，确认名称和主要节点与本卡一致；缺节点时先处理加载问题。
2. 在上面列出的入口替换素材并查看预览；多图、多音色、首尾帧先绑定各自职责。
3. 按当前输入方式填写文本或操作时间线；无需提示词的工作流跳过文字生成。
4. 保留模型、采样器与内部系统模板；只修改本任务需要的尺寸、时长或强度。先做一张图或一小段音视频。
5. 点击 ComfyUI 的运行/队列按钮，等待输出；失败时读取第一个报错节点，缺文件先换有效素材，显存不足先降低尺寸或片段长度。

实际可调参数位置（以当前界面值为准，首次不要全部修改）：

- #14 KSampler：`denoise`。

## 获取结果与验收

结果位置：#79 SaveImage。在对应预览保存/下载，核对本次输出时间。
对照处理前后的人脸、衣服和背景；视频逐段检查亮度跳变、光斑漂移。

失败重跑：保留原结果与种子记录，只调整一个明确变量；先判断是输入/节点错误还是画面、声音效果问题。

本卡依据工作流 JSON、节点公开字段和连线编写；尚未对本工作流执行 GPU 生成验收。用户实际 JSON 或界面变化时，以当前文件为准并重新核对。
