# 图生图-FLUX.2 Klein 4B 多图参考编辑

工作流路径：`图生图/图生图-FLUX.2 Klein 4B 多图参考编辑.json`  
核对：2026-09-20｜Flux / Kontext / Klein / Krea｜按字段填写  
源文件 SHA-256：`3366d703d96cd9423a3d64a219414b792b7e63c7ce286c565ae623c970c0ae0a`

## 用途与准备

按参考图定向编辑底图。
先确定底图、参考图各自职责与实际接入顺序。

## 本工作流先看

- #76 是图像1主体/底图，#81 是图像2属性参考；#92“第3步｜输入编辑命令并生成”的 text 填定向编辑指令。

## 素材放在哪里

| 位置 | 素材 | 说明 |
|---|---|---|
| #76 图片1｜主体与底图（必须替换） → `image` | 图片 | 替换示例素材并核对预览；多素材顺序按专用说明确认 |
| #81 图片2｜服装/场景/风格参考（必须替换） → `image` | 图片 | 替换示例素材并核对预览；多素材顺序按专用说明确认 |

## 提示词和填写位置

写“将参考图的哪些属性应用到底图的哪里，保留底图的哪些内容”，不写含糊的融合。

| 实际输入位置 | 用途 | 怎么填 |
|---|---|---|
| #92 第3步｜输入编辑命令并生成 → `text` | 文本（用途见专用说明） | 按用户目标填写；正文不含节点说明或尺寸参数 |

输入写法示例（用于说明表达方式；正式交付须替换为用户素材事实，并按本卡拆到对应字段）：

将参考物体放置在底图指定区域，匹配尺度、透视、光线和接触阴影，保持底图其余部分不变。正式文本必须注明每张图的职责。

## 参数与运行

1. 在工作流列表打开 `图生图/图生图-FLUX.2 Klein 4B 多图参考编辑.json`，确认名称和主要节点与本卡一致；缺节点时先处理加载问题。
2. 在上面列出的入口替换素材并查看预览；多图、多音色、首尾帧先绑定各自职责。
3. 按当前输入方式填写文本或操作时间线；无需提示词的工作流跳过文字生成。
4. 保留模型、采样器与内部系统模板；只修改本任务需要的尺寸、时长或强度。先做一张图或一小段音视频。
5. 点击 ComfyUI 的运行/队列按钮，等待输出；失败时读取第一个报错节点，缺文件先换有效素材，显存不足先降低尺寸或片段长度。

本卡没有从公开字段识别到需手动修改的数值入口；沿用当前预设，或在上方导演台/子图界面设置。

## 获取结果与验收

结果位置：#94 第4步｜查看结果并下载。在对应预览保存/下载，核对本次输出时间。
检查主体身份、背景、透视、参考属性与未要求改变的区域。

失败重跑：保留原结果与种子记录，只调整一个明确变量；先判断是输入/节点错误还是画面、声音效果问题。

本卡依据工作流 JSON、节点公开字段和连线编写；尚未对本工作流执行 GPU 生成验收。用户实际 JSON 或界面变化时，以当前文件为准并重新核对。
