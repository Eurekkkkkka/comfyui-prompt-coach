# 一键扩图-图片

工作流路径：`扩图/一键扩图-图片.json`  
核对：2026-09-20｜以本卡实际节点为准｜按字段填写  
源文件 SHA-256：`d8a2bc50fcb5fed58cedcbf4182dd4d554f2009480cc80dc1ff4d8f5bd2de783`

## 用途与准备

扩展原图画布并补全边缘内容。
上传原图，设置要扩展的方向和面积，保持主体占比合理。

## 本工作流先看

- 按下方实际输入位置完成一次小规模试跑；首次保留模型、采样器和内部系统模板，效果问题只改一个变量再比较。

## 素材放在哪里

| 位置 | 素材 | 说明 |
|---|---|---|
| #32 LoadImage → `image` | 图片 | 替换示例素材并核对预览；多素材顺序按专用说明确认 |

## 提示词和填写位置

描述新增区域应延续的背景、材质和光线，避免重复生成主体。

| 实际输入位置 | 用途 | 怎么填 |
|---|---|---|
| #10 CLIPTextEncode → `text` | 正向/编辑指令 | 按用户目标填写；正文不含节点说明或尺寸参数 |

输入写法示例（用于说明表达方式；正式交付须替换为用户素材事实，并按本卡拆到对应字段）：

向画布新增区域延续原有草地、远山与天空，保持地平线、透视与光照一致，不重复生成原图人物。

## 参数与运行

1. 在工作流列表打开 `扩图/一键扩图-图片.json`，确认名称和主要节点与本卡一致；缺节点时先处理加载问题。
2. 在上面列出的入口替换素材并查看预览；多图、多音色、首尾帧先绑定各自职责。
3. 按当前输入方式填写文本或操作时间线；无需提示词的工作流跳过文字生成。
4. 保留模型、采样器与内部系统模板；只修改本任务需要的尺寸、时长或强度。先做一张图或一小段音视频。
5. 点击 ComfyUI 的运行/队列按钮，等待输出；失败时读取第一个报错节点，缺文件先换有效素材，显存不足先降低尺寸或片段长度。

实际可调参数位置（以当前界面值为准，首次不要全部修改）：

- #34 ImagePadForOutpaint：`bottom`、`left`、`right`、`top`。
- #12 KSampler：`denoise`。

## 获取结果与验收

结果位置：#11 SaveImage。在对应预览保存/下载，核对本次输出时间。
检查四边接缝、透视、主体重复与最终尺寸。

失败重跑：保留原结果与种子记录，只调整一个明确变量；先判断是输入/节点错误还是画面、声音效果问题。

本卡依据工作流 JSON、节点公开字段和连线编写；尚未对本工作流执行 GPU 生成验收。用户实际 JSON 或界面变化时，以当前文件为准并重新核对。
