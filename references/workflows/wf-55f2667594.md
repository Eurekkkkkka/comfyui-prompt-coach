# 局部重绘-无痕改字

工作流路径：`局部重绘/局部重绘-无痕改字.json`  
核对：2026-09-20｜Qwen / Edit｜按字段填写  
源文件 SHA-256：`6f657db9ad43fa082dd42d39a2e2a27ce505d6f1b835e5383f68317756b04440`

## 用途与准备

在指定区域修复、消除、改字或合成。
准备原图与局部区域，使用画布或遮罩选定范围；融图变体另需要放入的素材。

## 本工作流先看

- #98 CR Text.text 是改字入口，例如“把指定区域文字改成‘春日茶会’，保持字体样式、笔触和透视不变”。#1.prompt 接入上游，不改 instruction 系统模板。

## 素材放在哪里

| 位置 | 素材 | 说明 |
|---|---|---|
| #117 LoadImage → `image` | 图片 | 实际连接标签：原始图像、遮罩；替换素材后核对预览 |

## 提示词和填写位置

明确修改区域的目标内容和边界外保持项；文字必须逐字给出。自动反推路线不要覆盖内部编码框。

| 实际输入位置 | 用途 | 怎么填 |
|---|---|---|
| #98 CR Text → `text` | 正向/编辑指令 | 按用户目标填写；正文不含节点说明或尺寸参数 |

已接线的文本框由上游提供，直接改内部旧值可能不生效：

- #1 TextEncodeQwenImageEditPlusAdvance_lrzjason → `prompt` ← #98 CR Text。修改上游创作入口；若来源是自动分析/构建器，在其实际输入或界面中修改。

输入写法示例（用于说明表达方式；正式交付须替换为用户素材事实，并按本卡拆到对应字段）：

将目标文字准确写成“春日茶会”，笔画完整清楚，保持指定的字体风格、布局和背景。

## 参数与运行

1. 在工作流列表打开 `局部重绘/局部重绘-无痕改字.json`，确认名称和主要节点与本卡一致；缺节点时先处理加载问题。
2. 在上面列出的入口替换素材并查看预览；多图、多音色、首尾帧先绑定各自职责。
3. 需要局部处理时，在实际画布/遮罩编辑入口选区，查看遮罩预览后再继续。下列节点用于定位选区处理链，不要求逐个编辑。
4. 保留模型、采样器与内部系统模板；只修改本任务需要的尺寸、时长或强度。先做一张图或一小段音视频。
5. 点击 ComfyUI 的运行/队列按钮，等待输出；失败时读取第一个报错节点，缺文件先换有效素材，显存不足先降低尺寸或片段长度。

实际可调参数位置（以当前界面值为准，首次不要全部修改）：

- #6 KSampler：`denoise`。

选区处理链定位：#28 GrowMaskWithBlur；#33 easy isMaskEmpty；#53 MaskPreview+。

## 获取结果与验收

结果位置：#120 SaveImage。在对应预览保存/下载，核对本次输出时间。
放大检查遮罩边缘、结构、文字和光影接缝，确认未影响区保持。

失败重跑：保留原结果与种子记录，只调整一个明确变量；先判断是输入/节点错误还是画面、声音效果问题。

本卡依据工作流 JSON、节点公开字段和连线编写；尚未对本工作流执行 GPU 生成验收。用户实际 JSON 或界面变化时，以当前文件为准并重新核对。
