# 多视角分镜-3D摄像机-Edit2511

工作流路径：`多视角分镜/多视角分镜-3D摄像机-Edit2511.json`  
核对：2026-09-20｜Qwen / Edit｜按字段填写  
源文件 SHA-256：`18318f930afd3061557d59601286477ed794661bcce9baf55ea96fef18c0ce01`

## 用途与准备

用同一角色或场景生成视角变化或资产视图。
提供主视觉或人物图；先列出需要的视角，保持身份与服装版本一致。

## 本工作流先看

- 先在 #108“控制角度”调 horizontal_angle、vertical_angle、zoom；生成的描述进入 #138.prompt_1，再进入 #68.prompt。
- prompt_2—prompt_5 是可选补充视角；未用时保持空白，不把每个空框都填满。#69 是负向。

## 素材放在哪里

| 位置 | 素材 | 说明 |
|---|---|---|
| #41 LoadImage → `image` | 图片 | 实际连接标签：原始图像；替换素材后核对预览 |

## 提示词和填写位置

视角变换与故事动作分开；写清摄像机移动方向和最终景别。固定四视图不需要新提示词。

| 实际输入位置 | 用途 | 怎么填 |
|---|---|---|
| #69 TextEncodeQwenImageEditPlus → `prompt` | 负向 | 首次保留当前负向；只为实际问题追加短约束 |
| #138 easy promptList → `prompt_2` | 正向/编辑指令 | 按用户目标填写；正文不含节点说明或尺寸参数 |
| #138 easy promptList → `prompt_3` | 正向/编辑指令 | 按用户目标填写；正文不含节点说明或尺寸参数 |
| #138 easy promptList → `prompt_4` | 正向/编辑指令 | 按用户目标填写；正文不含节点说明或尺寸参数 |
| #138 easy promptList → `prompt_5` | 正向/编辑指令 | 按用户目标填写；正文不含节点说明或尺寸参数 |

已接线的文本框由上游提供，直接改内部旧值可能不生效：

- #138 easy promptList → `prompt_1` ← #108 控制角度。修改上游创作入口；若来源是自动分析/构建器，在其实际输入或界面中修改。
- #68 TextEncodeQwenImageEditPlus (Positive) → `prompt` ← #138 easy promptList。修改上游创作入口；若来源是自动分析/构建器，在其实际输入或界面中修改。

输入写法示例（用于说明表达方式；正式交付须替换为用户素材事实，并按本卡拆到对应字段）：

Next Scene: 将摄像机移动到人物左前方，形成平视中景，保持人物身份、服装与场景陈设一致。

## 参数与运行

1. 在工作流列表打开 `多视角分镜/多视角分镜-3D摄像机-Edit2511.json`，确认名称和主要节点与本卡一致；缺节点时先处理加载问题。
2. 在上面列出的入口替换素材并查看预览；多图、多音色、首尾帧先绑定各自职责。
3. 按当前输入方式填写文本或操作时间线；无需提示词的工作流跳过文字生成。
4. 保留模型、采样器与内部系统模板；只修改本任务需要的尺寸、时长或强度。先做一张图或一小段音视频。
5. 点击 ComfyUI 的运行/队列按钮，等待输出；失败时读取第一个报错节点，缺文件先换有效素材，显存不足先降低尺寸或片段长度。

实际可调参数位置（以当前界面值为准，首次不要全部修改）：

- #65 KSampler：`denoise`。
- #108 QwenMultiangleCameraNode：`horizontal_angle`、`vertical_angle`、`zoom`。

## 获取结果与验收

结果位置：#123 保存图片。在对应预览保存/下载，核对本次输出时间。
核对角色脸、衣服、道具一致，分清不同视角与重复同一图；场景不要变成拼贴。

失败重跑：保留原结果与种子记录，只调整一个明确变量；先判断是输入/节点错误还是画面、声音效果问题。

本卡依据工作流 JSON、节点公开字段和连线编写；尚未对本工作流执行 GPU 生成验收。用户实际 JSON 或界面变化时，以当前文件为准并重新核对。
